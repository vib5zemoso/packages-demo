name: My First Worflow

on: push

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
    - name: Welcome message
      run: echo "My first Github Action"

    - name: List files
      run: ls

    - name: Read file
      run: car README.md